name = "agentops-ebook"
compatibility_date = "2024-11-01"
# Deploy prebuilt static files from repo root (index.html + assets/).
# Cloudflare Git builds were failing because `dist/` is gitignored and no build ran.
pages_build_output_dir = "."

[build]
command = "npm ci && npm run build"
